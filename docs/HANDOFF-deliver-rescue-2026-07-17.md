# HANDOFF — Deliver (endure-mind) rescue → monetizable

**Date:** 2026-07-17 · **Repo:** `~/endure-plan-engine` · **Branch at handoff:** `feat/media-radar` (dirty)
**Mission:** Evaluation of Deliver vs Dirt Craft design standards is DONE (see memory `project_endure-mind.md`). Rescue-and-fix sequence was STARTED and stopped after step 1. Orchestration model: Claude = boss/reviewer, Codex 5.6 = implementation worker (token economy).

---

## 1. What is true right now (verified this session)

### The shipping artifact exists but is orphaned
- All Deliver ship-work sits **UNCOMMITTED** in `~/endure-plan-engine` working tree on branch `feat/media-radar`, mixed with unrelated media-radar/feed changes. Nothing Deliver-related has ever been committed (`git log --all -- data/courses/deliver` is empty).
- **Untracked:** `data/courses/deliver/` (course.json + 38 lesson JSONs + stripe-manifest.json + asset-manifest.json), `scripts/create_deliver_stripe_product.py`, `scripts/generate_deliver_audio_placeholders.py`, `scripts/fix_feed_403.py`
- **Modified (mixed bag, needs partition — see §4 step 3):** `wordpress/generate_courses.py`, `wordpress/generate_guide.py` (+231, holds `BLOCK_RENDERERS`), `wordpress/brand_tokens.py` (clean-pro theme), `workers/course-access/worker.js` (Deliver XP levels Aware→Attuned→Focused→Composed→Delivered), `wordpress/generate_homepage.py` (2 lines), `wordpress/mu-plugins/gg-header.php` (2 lines), `scripts/push_wordpress.py`, plus media-radar-scoped: `scripts/generate_llms_txt.py`, `scripts/generate_rss_feed.py`, `web/llms.txt`, `web/robots.txt`, `web/sitemap.xml`, `web/feed/races.xml → web/race-feed.xml` (rename), tests.

### Monetization wiring — already real
- **Live Stripe product**: $79 · product `prod_U8uCPKvxGxgePe` · price `price_1TAcO5LoaHDbEqSqMiwuEi13` · payment link `https://buy.stripe.com/6oUaEYaaW48R2xh589fjG00` (in `data/courses/deliver/stripe-manifest.json` + `course.json`).
- `course.json`: `status: "active"`, `noindex: true`, `theme: "clean-pro"`, price_usd 79.
- Theme `clean-pro` = full `--gg-*` token remap in `brand_tokens.py` (`get_clean_pro_tokens_css()` ~line 370): Inter, white/#fafafa, accent #4ECDC4. All existing Gravel God block renderers work unmodified under it.
- All Deliver block types are covered by `generate_guide.py` `BLOCK_RENDERERS` (~line 882): prose, callout, knowledge_check, tabs, accordion, image, process_list, audio, timeline, flashcard, sorting, scenario, download.

### Build & deploy state
- **BUILD IS BROKEN**: `python3 wordpress/generate_courses.py` → `ImportError: cannot import name 'get_site_header_js' from 'shared_header'`. Function exists NOWHERE (checked `.claude/worktrees/jolly-volhard` too) — partial parallel-session state. This is fix #1.
- Stale build output exists at `wordpress/output/course/deliver/` (38 lesson pages, dated ~Mar 13) — do not trust it; rebuild.
- **Live**: `gravelgodcycling.com/course/deliver/` = **404**. Live `/course/` index lists dirt-craft + gravel-hydration-mastery only. Local built index DOES list deliver (it's included once status=active).
- Deploy path: `python3 scripts/push_wordpress.py --sync-courses` (tar+ssh to SiteGround `~/www/gravelgodcycling.com/public_html/course`). Worker deploy (Cloudflare) needed for gating/XP changes in `workers/course-access/worker.js`.

### Asset truth vs sales copy
- 8 concept diagrams (yerkes-dodson, identity-tree, flow-channel, etc.): **REAL** — hand-built inline SVGs rendered from `asset_id`, Clean Pro palette. Done.
- 6 audio MP3s in output: **silent identical 16,263-byte placeholders** (generated by `scripts/generate_deliver_audio_placeholders.py`). Sales copy promises "8 guided audio exercises". 8 finished narration scripts exist at `~/endure-mind/scripts/audio/`. ElevenLabs pipeline specced in `~/endure-mind/PRODUCTION.md` (Matt's cloned voice, stability 0.5/similarity 0.75, ~$30–50).
- Downloads: 8 **HTML** worksheets in output, but `asset-manifest.json` claims `.pdf` paths. Check lesson `download` blocks and reconcile.
- Copy bugs: `~/endure-mind/course/deliver.json` description says "30 lessons" (actual: 38 incl. 6 quizzes). Pipeline `course.json` description says "8 guided audio exercises" (6 wired, 0 real).

### Source-of-truth relationships
- `~/endure-mind` (clean, pushed, `wattgod/endure-mind`) = content source of truth. `course/deliver.json` (501 blocks) was ported 1:1 into `data/courses/deliver/lessons/*.json` with block-type renames: `text→prose`, `quiz→knowledge_check`. Verified in sync (38/38 lessons, matching per-lesson block counts).
- Dirt Craft benchmark repo: `~/Documents/GravelGod/dirt-craft-course` (live at `/course/dirt-craft/`).

## 2. Evaluation verdict (already delivered to Matt)

Meets bar: voice (Stoic Coach ≈ reformed-roadie craft), Named Tools via `process` blocks (Grit Stack, 6-2-7, Daily 5, Performance Statements, Highlight Reel), arc-named modules, real Stripe (ahead of Dirt Craft's placeholders), 120-citation research base.
Below bar (ALL post-revenue, non-blocking): no lesson-00 practice-method meta-tool (Practice Loop equivalent); no drill/proof_gate/commitment/sensation_target equivalents (practice assignments live in prose/callouts); callout saturation 228/501 = 45% (anti-slop violation); module quizzes not verified cumulative (Dirt Craft stack-checks retrieval-test ALL prior tools); citations not surfaced in-lesson. Theme divergence (Clean Pro vs Desert Editorial) is deliberate — keep, since Deliver sells across all Endure businesses.

## 3. Done this session

1. Full evaluation (two Explore-agent surveys + direct pipeline verification).
2. Memory updated: `project_endure-mind.md` (full state section) + `MEMORY.md` index line (⚠️ uncommitted work warning).
3. **Safety backup taken**: `~/.claude/backups/deliver-rescue-2026-07-17.tgz` (449 KB, all modified+untracked files, relative paths — restore: `tar -xzf ~/.claude/backups/deliver-rescue-2026-07-17.tgz -C ~/endure-plan-engine`).
4. This handoff.

**Nothing else has been executed. No branch created, no commits, no fixes applied, no deploys.**

## 4. Next steps — exact sequence

> Boss/worker protocol: Claude inspects+decides+reviews; Codex implements. Codex invocation (cask 0.144.0 lacks code-mode host; reaper kills processes whose **argv** matches agent-prompt patterns, so pipe prompts via **stdin**):
> ```bash
> cat /path/to/prompt.md | codex exec --full-auto -c features.code_mode_host=false -m gpt-5.6-sol -
> ```

1. **Create rescue branch.** Try `git checkout -b feat/deliver-course main` (cleaner PR base). If checkout conflicts on modified files, fall back to `git checkout -b feat/deliver-course` from current HEAD (drags media-radar commits into the PR — acceptable; check first whether `git log main..feat/media-radar` is already merged). Working tree carries over either way.
2. **Fix the build break** (Codex): inspect how `generate_courses.py` uses `get_site_header_js`; either implement it in `wordpress/shared_header.py` (pattern-match `get_site_header_html`/`get_site_header_css` — likely the mobile-nav toggle JS) or remove the import+call if the JS is already inlined elsewhere. Acceptance: `python3 wordpress/generate_courses.py` exits 0, builds deliver + gravel-hydration-mastery, **zero** "WARNING: Unknown block type", 38 deliver lesson dirs in `wordpress/output/course/deliver/lesson/`.
3. **Partition & commit** (Claude reviews diffs, decides file list). Deliver-scoped set (verify each diff hunk before staging — `generate_guide.py`, `push_wordpress.py`, `generate_homepage.py`, `gg-header.php` may contain mixed hunks; use `git add -p` where needed): `data/courses/deliver/`, `scripts/create_deliver_stripe_product.py`, `scripts/generate_deliver_audio_placeholders.py`, `wordpress/brand_tokens.py`, `wordpress/generate_courses.py`, deliver hunks of `wordpress/generate_guide.py`, `workers/course-access/worker.js`, plus the shared_header fix. Leave media-radar/feed changes uncommitted in the tree (they belong to another in-flight session — DO NOT revert or commit them). **Pre-commit gate runs build/tests — do NOT `--no-verify`; fix instead.** Commit message: `feat: add Deliver course (port from endure-mind) with clean-pro theme + Stripe wiring`.
4. **Push branch** for durability (`git push -u origin feat/deliver-course`), then verify push landed (memory: `feedback_verify-push-landed.md`). Do NOT open/arm an auto-merge PR (memory: auto-merge consumes intermediate commits).
5. **Copy fixes** (Codex, then commit): `~/endure-mind/course/deliver.json` "30 lessons" → "32 lessons + 6 module quizzes" (or "38 lessons"); pipeline `course.json` audio claim — pending Matt's audio decision (see §5), safest interim: "guided audio exercises" without a count, or remove until real; reconcile downloads `.pdf` claims vs `.html` files (relabel as worksheets OR generate real PDFs). Mirror any content fix in BOTH `~/endure-mind` (source of truth, commit+push there too) and the pipeline port.
6. **Rebuild + spot-verify rendering**: open 3–4 built lesson pages (e.g. `the-arousal-dial` for image/accordion, `deliver` [05-04] for tabs/process/audio, a quiz lesson) — check Clean Pro tokens applied, quiz JS works, no unstyled blocks.
7. **STOP — Matt sign-off gates:**
   - **Audio**: record 8 via ElevenLabs (scripts ready, ~1 day, recommended) vs cut claim for v1.
   - **Deploy**: Cloudflare worker deploy + `push_wordpress.py --sync-courses` + live e2e purchase test through the real Stripe link + unlock flow.
   - **Soft launch**: email drip (`~/endure-mind/scripts/email-drip-sequence.md`) to Gravel Grit alumni; keep `noindex: true` until full launch.

## 5. Open decisions for Matt

1. Audio: record now (recommended — differentiating, cheap, copy already promises it) or strip claim and ship without.
2. Deploy sign-off (production WordPress + Cloudflare worker) — not to be done autonomously.
3. Post-revenue craft sprint (lesson-00, practice gates/commitments, callout diet, cumulative stack-checks) — schedule after first sales.

## 6. Hazards

- **Concurrent-session branch hazard**: the media-radar changes in this tree likely belong to another in-flight session. Touch ONLY deliver-scoped files. Never `git checkout .` / `git reset --hard`.
- Pre-commit hook eats MERGE_HEAD during merges; gate blocks on build/test fail.
- tsc-reaper kills processes by argv prompt match — stdin-pipe all agent prompts.
- Stripe link is LIVE mode — a real e2e purchase charges a real card; refund after test.
- `.claude/worktrees/jolly-volhard/` exists in-tree (another session's worktree) — ignore it.
